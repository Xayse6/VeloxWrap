CREATE TABLE usuario (
    id_usuario      INTEGER GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    nome            VARCHAR(100) NOT NULL,
    admin           BOOLEAN NOT NULL DEFAULT FALSE,
    data_nascimento DATE NOT NULL CHECK (data_nascimento < CURRENT_DATE),
    cpf             VARCHAR(11) UNIQUE NOT NULL CHECK (length(cpf) = 11),
    email           VARCHAR(100) UNIQUE NOT NULL,
    senha           VARCHAR(255) NOT NULL,
    data_cadastro   TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


CREATE TABLE marca (
    id_marca    INTEGER GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    nome_marca  VARCHAR(50) NOT NULL UNIQUE,
    sigla_marca VARCHAR(10) NOT NULL UNIQUE
);



CREATE TABLE modelo (
    id_modelo       INTEGER GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    nome_modelo     VARCHAR(80) NOT NULL,
    ano_lancamento  INT NOT NULL CHECK (ano_lancamento BETWEEN 1886 AND EXTRACT(YEAR FROM CURRENT_DATE)+2),
    id_marca        INT NOT NULL,

    CONSTRAINT fk_modelo_marca
        FOREIGN KEY (id_marca)
        REFERENCES marca(id_marca)
        ON DELETE RESTRICT,

    CONSTRAINT uk_modelo UNIQUE (id_marca, nome_modelo, ano_lancamento)
);


CREATE TABLE veiculo (
    id_veiculo    INTEGER GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    id_usuario    INT NOT NULL,
    id_modelo     INT NOT NULL,
    placa         VARCHAR(8) UNIQUE,
    cor           VARCHAR(30) NOT NULL,
    apelido       VARCHAR(50),
    data_cadastro TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT fk_veiculo_usuario
        FOREIGN KEY (id_usuario)
        REFERENCES usuario(id_usuario)
        ON DELETE CASCADE,

    CONSTRAINT fk_veiculo_modelo
        FOREIGN KEY (id_modelo)
        REFERENCES modelo(id_modelo)
        ON DELETE RESTRICT
);




INSERT INTO usuario (nome, admin, data_nascimento, cpf, email, senha)
VALUES
('João José Gomes da Silva', FALSE, '1990-08-10', '08243060073', 'joaojosegomes@gmail.com', '1234567890'),
('Guilherme', TRUE, '2006-05-26', '12345678901', 'admin@gmail.com', '123456');

INSERT INTO marca (nome_marca, sigla_marca) VALUES
('Volkswagen','VW'),
('Toyota', 'TYT'),
('Chevrolet', 'GM'),
('Honda', 'HON');

INSERT INTO modelo (nome_modelo, ano_lancamento, id_marca) VALUES
('Gol', 2022, 1),
('Corolla', 2019, 2),
('Onix', 2019, 3),
('Astra', 2009, 3),
('Civic', 2020, 4);

INSERT INTO veiculo (id_usuario, id_modelo, placa, cor)
VALUES (1, 3, 'ABC1D23', 'Branco');